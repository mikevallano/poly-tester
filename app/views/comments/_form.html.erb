<%= form_for @comment do |f| %>
  <%= f.label :body %>
  <%= f.text_field :body %>
  <%= f.hidden_field :commentable_type, value: @commentable_type %>
  <%= f.hidden_field :commentable, value: @commentable.id %>
  <%= f.submit %>
<% end %>
